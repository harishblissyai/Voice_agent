services:
  blissy-voice:
    build: .
    container_name: blissy-voice-bot
    ports:
      - "8070:8070"       # host:container
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
